<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.NoActionBar">
    </style>
    <style name="AppTheme.AppBarOverlay" parent="android:Theme.NoTitleBar.Fullscreen"/>
    <style name="AppTheme.PopupOverlay" parent="android:Theme.NoTitleBar.Fullscreen"/>

</resources>
